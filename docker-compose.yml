version: '3'

services:

    bot:
        image: effprime/basement-bot:${TAG:-latest}
        container_name: basement_bot
        stop_signal: SIGINT
        restart: unless-stopped
        environment:
            - DEBUG=0
